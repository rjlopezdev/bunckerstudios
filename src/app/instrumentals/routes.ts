import { Routes } from '@angular/router';
import { InstrumentalsComponent } from './instrumentals/instrumentals.component';

export const routes : Routes = [
    {
        path: "",
        component: InstrumentalsComponent
    }
]